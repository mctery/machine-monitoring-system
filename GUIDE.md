# 🚀 TMOT Machine Monitoring - คู่มือเริ่มต้นใช้งาน

## 📋 สารบัญ
1. [ติดตั้ง Node.js](#ติดตั้ง-nodejs)
2. [ติดตั้ง Project](#ติดตั้ง-project)
3. [รัน Project](#รัน-project)
4. [การใช้งาน](#การใช้งาน)
5. [Troubleshooting](#troubleshooting)

---

## ติดตั้ง Node.js

### Windows
1. ดาวน์โหลด Node.js จาก https://nodejs.org/ (เลือก LTS version)
2. รัน installer และติดตั้งตามขั้นตอน
3. เปิด Command Prompt และตรวจสอบ:
```bash
node --version
npm --version
```

### macOS
```bash
# ใช้ Homebrew
brew install node

# หรือดาวน์โหลดจาก nodejs.org
```

### Linux
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install nodejs npm

# Verify
node --version
npm --version
```

---

## ติดตั้ง Project

### 1. Extract โปรเจค
```bash
# Extract zip file
unzip tmot-monitoring.zip
cd tmot-monitoring
```

### 2. ติดตั้ง Dependencies
```bash
npm install
```

**รอสักครู่** - จะใช้เวลาประมาณ 2-3 นาที ขึ้นอยู่กับความเร็ว internet

### 3. ตรวจสอบการติดตั้ง
```bash
npm list --depth=0
```

ควรเห็น packages เหล่านี้:
- react@18.3.1
- react-dom@18.3.1
- react-router-dom@6.26.0
- zustand@4.5.4
- tailwindcss@3.4.10
- vite@5.4.0

---

## รัน Project

### Development Mode
```bash
npm run dev
```

คุณจะเห็น:
```
  VITE v5.4.0  ready in 500 ms

  ➜  Local:   http://localhost:5000/
  ➜  Network: http://192.168.1.x:5000/
```

เปิดเบราว์เซอร์ไปที่: **http://localhost:5000**

### Production Build
```bash
# Build project
npm run build

# Preview production build
npm run preview
```

---

## การใช้งาน

### 🏠 หน้าหลัก - Timeline Viewer

**จุดเด่น:**
- แสดง timeline การทำงานเครื่องจักร
- เลือกช่วงเวลาได้
- Export ข้อมูล

**วิธีใช้:**
1. เลือกวันที่เริ่มต้น (From)
2. เลือกวันที่สิ้นสุด (To)
3. กด **UPDATE** เพื่อดูข้อมูล
4. กด **EXPORT** เพื่อดาวน์โหลด CSV

**อ่านสีใน Timeline:**
- 🟢 สีเขียว = เครื่องกำลังทำงาน (RUN)
- 🟡 สีเหลือง = เครื่องหยุด (STOP)
- 🔵 สีฟ้า = เครื่องว่าง (IDLE)

---

### 📊 Machine Status

**จุดเด่น:**
- ดูสถานะเครื่องทั้งหมด
- Filter ตาม group
- เช็ค performance ratios

**วิธีใช้:**
1. เลือก Group จาก dropdown (ALL, PIS, SECTOR, etc.)
2. ดูตาราง:
   - **State**: สถานะปัจจุบัน
   - **STOP Hours**: ชั่วโมงที่หยุด
   - **Ratios**: เปรียบเทียบ actual vs target

**อ่านสี:**
- ⚪ พื้นขาว = ดีมาก (≥80%)
- 🟡 พื้นเหลือง = พอใช้ (50-80%)
- 🔴 พื้นแดง = ต่ำกว่าเป้า (<50%)

---

### ⚙️ Setup

**จุดเด่น:**
- ตั้งค่า target ratio
- แก้ไขได้ทีละเครื่อง

**วิธีใช้:**
1. กด **Select** ที่เครื่องที่ต้องการแก้
2. ใส่ค่า Weekly Target Ratio
3. ใส่ค่า Monthly Target Ratio
4. กด **Save** เพื่อบันทึก หรือ **Cancel** เพื่อยกเลิก

---

### 📞 Contact

**ข้อมูล:**
- ชื่อบริษัท
- ระบบที่ใช้
- วิธีติดต่อ support

---

## 🎯 การทำงานของระบบ

### State Management
ใช้ Zustand เก็บข้อมูล:
```typescript
- machines: ข้อมูลเครื่องจักรทั้งหมด
- timelineData: ข้อมูล timeline
- selectedGroup: group ที่เลือก
- dateRange: ช่วงเวลาที่เลือก
```

### Mock Data
ปัจจุบันใช้ข้อมูลจำลอง (Mock) ซึ่งอยู่ใน:
```
src/data/mockData.ts
```

มีข้อมูล:
- 19 เครื่องจักร
- 4 groups (PIS, SECTOR, SIDE MOLD, BLADE)
- Timeline data แบบสุ่ม

### การ Export
- ปุ่ม EXPORT จะสร้างไฟล์ CSV
- ดาวน์โหลดอัตโนมัติ
- ชื่อไฟล์: `machine-data-YYYY-MM-DD.csv`

---

## Troubleshooting

### ❌ ปัญหา: Port 5000 ถูกใช้งานแล้ว

**วิธีแก้:**
```bash
# แก้ไข vite.config.ts
server: {
  port: 3000,  // เปลี่ยนเป็น port อื่น
}
```

### ❌ ปัญหา: npm install error

**วิธีแก้:**
```bash
# ลบ node_modules และ install ใหม่
rm -rf node_modules
rm package-lock.json
npm install
```

### ❌ ปัญหา: หน้าจอว่างเปล่า

**ตรวจสอบ:**
1. เปิด Console (F12) ดู error
2. ตรวจสอบว่า port ถูกต้อง
3. รัน `npm run dev` ใหม่

### ❌ ปัญหา: Build error

**วิธีแก้:**
```bash
# Clear cache
npm run clean
npm run build
```

### ❌ ปัญหา: TypeScript errors

**วิธีแก้:**
```bash
# ตรวจสอบ TypeScript
npx tsc --noEmit
```

---

## 📱 Browser Support

| Browser | Version | Status |
|---------|---------|--------|
| Chrome | Latest | ✅ Full Support |
| Firefox | Latest | ✅ Full Support |
| Safari | Latest | ✅ Full Support |
| Edge | Latest | ✅ Full Support |

**แนะนำ:** Chrome หรือ Edge สำหรับประสบการณ์ที่ดีที่สุด

---

## 🔧 Configuration

### เปลี่ยน Port
แก้ไข `vite.config.ts`:
```typescript
server: {
  port: 3000,  // เปลี่ยนตรงนี้
}
```

### เปลี่ยนสี Theme
แก้ไข `tailwind.config.js`:
```javascript
theme: {
  extend: {
    colors: {
      // เพิ่มสีของคุณ
    }
  }
}
```

### เพิ่ม Mock Data
แก้ไข `src/data/mockData.ts`:
```typescript
export const mockMachines: Machine[] = [
  // เพิ่มข้อมูลเครื่องใหม่ตรงนี้
];
```

---

## 📊 Performance Tips

### 1. การโหลดเร็วขึ้น
```bash
# Build และ optimize
npm run build
```

### 2. ลด Bundle Size
- ใช้ lazy loading สำหรับ components
- Import เฉพาะที่จำเป็น

### 3. Cache Management
- Browser จะ cache static files
- Clear cache เมื่อ update version ใหม่

---

## 🔐 Security

### สำหรับ Production:
1. เพิ่ม Authentication
2. ใช้ HTTPS
3. Implement CORS
4. Rate limiting
5. Input validation

### Environment Variables:
สร้างไฟล์ `.env`:
```bash
VITE_API_URL=https://api.example.com
VITE_APP_NAME=TMOT Monitoring
```

---

## 📚 เอกสารเพิ่มเติม

### Official Docs:
- [React](https://react.dev/)
- [TypeScript](https://www.typescriptlang.org/)
- [Vite](https://vitejs.dev/)
- [Tailwind CSS](https://tailwindcss.com/)
- [Zustand](https://zustand-demo.pmnd.rs/)

### Tutorials:
- React Basics: https://react.dev/learn
- TypeScript Handbook: https://www.typescriptlang.org/docs/
- Tailwind CSS Tutorial: https://tailwindcss.com/docs

---

## 💬 FAQ

**Q: ข้อมูลเก็บที่ไหน?**
A: ปัจจุบันใช้ mock data ใน memory เท่านั้น ไม่มีการเก็บใน database

**Q: สามารถ connect database ได้ไหม?**
A: ได้ครับ ต้อง implement backend API แล้ว integrate

**Q: รองรับ mobile ไหม?**
A: ออกแบบสำหรับ desktop เป็นหลัก แต่สามารถใช้บน tablet ได้

**Q: จะเพิ่มเครื่องใหม่ยังไง?**
A: แก้ไขใน `src/data/mockData.ts` หรือ integrate กับ backend API

**Q: Export รูปแบบอื่นได้ไหม?**
A: ปัจจุบัน support CSV เท่านั้น แต่สามารถเพิ่ม Excel/PDF ได้

---

## ✅ Checklist ก่อนใช้งาน

- [ ] ติดตั้ง Node.js แล้ว
- [ ] ติดตั้ง dependencies แล้ว (`npm install`)
- [ ] รัน dev server ได้ (`npm run dev`)
- [ ] เปิด browser ได้ (http://localhost:5000)
- [ ] เห็นข้อมูลใน table
- [ ] Timeline แสดงผลถูกต้อง
- [ ] สามารถ export CSV ได้

---

## 🎓 ขั้นตอนถัดไป

1. **ทดลองใช้งาน** - เล่นดูทุกฟีเจอร์
2. **ปรับแต่ง Mock Data** - ใส่ข้อมูลที่เหมือนจริง
3. **เตรียม Backend API** - ถ้าต้องการ real-time data
4. **Deploy** - วางบน production server
5. **Monitor** - ติดตามการใช้งาน

---

**🎉 ขอให้ใช้งานสนุก!**

หากมีปัญหาหรือข้อสงสัย ติดต่อ: system.admin@tmot.co.th
